{
  "name": "render-root-shim",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "python3 -m pip install --no-cache-dir Flask==3.1.3 gunicorn==23.0.0 flask-cors==6.0.2 flask-debugtoolbar==0.16.0 requests==2.31.0",
    "postinstall": "npm run build",
    "start": "PYTHONPATH=appwrite gunicorn backend.webapp:app --bind 0.0.0.0:${PORT:-8000} --workers 2"
  }
}
